# platforms

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build platforms` to build the library.

## Running unit tests

Run `nx test platforms` to execute the unit tests via [Vitest](https://vitest.dev/).
